%h3 Sondage 
%ul.nav.nav-pills
  - @sondages.each do |s|
    - if s.questions.count > 0
      - if s.users.include? current_user
        %li.disabled{:role => "presentation"}
          %a{:href => "/"}
            = s.nom
            par
            = s.entreprise.nom
      - else
        %li{:role => "presentation"}
          = link_to s.nom + " par " + s.entreprise.nom, s
